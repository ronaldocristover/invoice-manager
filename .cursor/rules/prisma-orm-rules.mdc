---
description: Defines that prisma should be considered as ORM.
globs: prisma/schema.prisma
---
- Prisma is being used as an ORM.
- Default Database Schema is MySQL
- Put Timestamp created_at and updated_at for each table, created_at default is now and updated_at is default null, but will updated if there some change on the row
- Make sure to add index and composite index.